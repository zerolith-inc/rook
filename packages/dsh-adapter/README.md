# DSH adapter

Reserved integration point for DSH's headless agent kernel. No runtime is connected yet.

Before implementation, identify the upstream package and supported headless API,
verify Bun compatibility, and confirm session/workflow persistence and recovery.
DSH owns agent loops, sessions, tools, skills, subagents, and workflows.
